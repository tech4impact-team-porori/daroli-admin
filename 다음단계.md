# 다음 단계

> 이 파일은 작업하면서 계속 고쳐 씁니다. 세션 끝날 때 "다음에 할 일"을 한 줄 적어두세요.

---

## ✅ Phase 2 — 완료 (2026-08-15)

`lib/data/mock.ts` 작성 완료 — 매니저 7명(각남면·각북면·이서면·운문면 매니저 0명), 대상자 22명(13명 미배정),
요청 15건, 활동일지 55건(수동완료 8건 포함), 정산/출금은 `lib/calc.ts`로만 집계. `lib/data/index.ts`의 `db`를
mock에 연결. 임시 스크립트로 잔액 음수 없음·지역 부족 판정·재산정 idempotent 확인 완료 → 커밋함.

**다음에 할 일:** Phase 3 — `lib/calc.ts` vitest 테스트 작성 (경계 조건: 잔액 0/초과신청/승인취소 음수/단가변경 후 재산정/합계 검증)

---

## ✅ Phase 1 — 완료 (2026-08-15)

Next.js(App Router) + TypeScript + Tailwind v4 + shadcn/ui 셋업, 사이드바 7개 메뉴 + 상단 바 placeholder,
7개 경로 빈 페이지, Pretendard 폰트 적용. 브라우저에서 7개 메뉴 이동 확인 완료 → 커밋함 (`feature/login-api`, root commit).

---

## ✅ Phase 0 — 완료 (2026-08-13)

| 파일 | 내용 |
|---|---|
| `CLAUDE.md` | Claude Code가 매 세션 자동으로 읽는 규약 |
| `lib/status.ts` | 상태값 — **백엔드 확정되면 이 파일만 고침** |
| `lib/types.ts` | 데이터 타입 (기획서 화면 명세 기준) |
| `lib/calc.ts` | 금액 계산식 (예산 4단계·잔액·지역 판정) |
| `lib/data/index.ts` | 데이터 접근 인터페이스 |
| `docs/` | 기획서·백엔드 가이드 사본 |

---

## ▶ Phase 1 — 지금 할 것

터미널에서 이 폴더를 열고 Claude Code를 실행한 뒤, 아래를 그대로 붙여넣으세요.

```
CLAUDE.md 와 lib/ 폴더를 먼저 읽어줘. 오늘은 Phase 1만 한다.

할 것:
1. 이 폴더에 Next.js(App Router) + TypeScript + Tailwind 프로젝트를 세팅해줘.
   이미 있는 lib/ 폴더와 CLAUDE.md, docs/, 다음단계.md 는 지우지 말고 그대로 살려줘.
2. shadcn/ui 초기화. 지금 필요한 컴포넌트만: button, card, badge, table, tabs, dialog, input, select
3. 앱 껍데기:
   - 좌측 고정 사이드바, 메뉴 7개
     대시보드(/) / 요청 현황(/requests) / 일지 검토(/review) /
     대상자 관리(/recipients) / 매니저 관리(/managers) /
     활동비 관리(/payments) / 설정(/settings)
   - 상단 바: 기간 필터 자리(placeholder), 개인정보 보호 모드 토글 자리, 계정 메뉴 자리
   - 현재 경로 메뉴 활성 표시
4. 7개 경로에 빈 페이지 (h1 제목만)
5. Pretendard 폰트 적용, 숫자에 tabular-nums

오늘은 데이터도 스타일 디테일도 넣지 마. 클릭해서 이동되는 뼈대까지만.
상단 바의 토글·필터는 동작 없이 자리만 잡아줘.

먼저 계획을 보여주고, 내가 확인한 다음에 실행해.
```

**끝나면:** 브라우저에서 7개 메뉴가 다 눌리는지 확인 → `git init` 후 커밋

---

## 이후 순서

| Phase | 할 것 | 참고 |
|---|---|---|
| 2 | 가짜 데이터 (`lib/data/mock.ts`) — 매니저 6명, 대상자 20여명, 일지 50여건 | 아래 주의사항 |
| 3 | `calc.ts` 테스트 (vitest) | 이미 구현돼 있음. 테스트만 |
| 4 | `admin-review` 일지 검토 ★ | 기획서 2-1 |
| 5 | `admin-payments` + `admin-settings` ★ | 기획서 2-2, 2-3 |
| 6 | `admin-managers` + `admin-recipients` | 기획서 2-4, 2-5 |
| 7 | 대시보드 3탭 | 기획서 2-6~2-8 |
| 8 | 통합 지역 지도 | 기획서 2-9. GeoJSON 필요 |
| 9 | `admin-requests` | 기획서 2-10 |
| 10 | 백엔드 DB 연결 (`lib/data/real.ts`) | |

### Phase 2 가짜 데이터 주의

- **실제 대상자·매니저 정보 금지.** `김○○`, `어르신A` 같은 가명만
- **일부러 불균형하게** 만들 것. 데이터가 고르면 "매니저 부족 지역" 판정이 안 걸려서
  지도 로직을 검증할 수 없습니다. **매니저 0명인 읍·면을 반드시 몇 개** 만들 것
- 매니저 실제 분포 참고: 화양읍 1 · 청도읍 3 · 매전면 1 · 금천면 1 · 풍각면 1
- 일지 상태를 골고루: 검토 대기 / 승인 / 반려
- GPS 없는 수동 완료 건, 담당 매니저 미배정 대상자도 섞을 것 (화면이 안 깨지는지 확인용)

---

## 백엔드 담당자에게 물어볼 것

**지금 당장 (이것만 오면 임시값을 확정으로 바꿉니다)**

```
일지 상태값 뭐로 내려오나요? pending / approved / rejected 로 생각하고 있는데 맞나요?
출금은 requested / paid / rejected, 정산은 calculated / approved 로 볼게요.
매니저 비활성은 inactive 맞나요?

※ applied/educated/active 랑 requested/proposed/confirmed/completed/cancelled 는
  기획서 Ver1.0에 있어서 그대로 쓰겠습니다
```

**답이 오면:** `lib/status.ts` 의 🟨 표시된 값만 고칩니다. 다른 파일은 손대지 않습니다.

나머지 질문 21개는 `docs/백엔드_소통가이드.md` 5장에 정리돼 있습니다. 급하지 않습니다.

**기획팀에 확인할 것**
- 활동 유형이 3종(일반/공동체/긴급)인지 5종(+픽업/기타)인지
- 요청 유형 ↔ 활동 유형 변환 규칙

---

## 내가 준비할 것

- [ ] 청도군 9개 읍·면 GeoJSON — Phase 8 전까지
      (행정안전부 도로명주소 전자지도 / SGIS / GitHub `vuski/admdongkor`)
      전국 파일에서 **청도군만 잘라내서** 용량 줄일 것
- [ ] 백엔드 담당자에게 상태값 질문 보내기

---

## 세션 시작 루틴

1. `git status` 로 깨끗한지 확인
2. **이번 세션에 만들 화면 하나**를 정한다
3. `docs/관리자대시보드_제작기획.md` 에서 그 화면 명세를 복사해 프롬프트에 붙인다
4. 계획 먼저 받고 → 읽고 → 고치고 → 실행
5. 브라우저 확인 → 커밋 → 이 파일에 다음 할 일 한 줄 적기
