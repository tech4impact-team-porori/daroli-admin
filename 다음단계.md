# 다음 단계

> 이 파일은 작업하면서 계속 고쳐 씁니다. 세션 끝날 때 "다음에 할 일"을 한 줄 적어두세요.

---

## ▶ Phase 10 — 지금 할 것: 저장소 만들고 팀에 공유

**전체 로드맵은 `docs/운영투입_로드맵.md` 참고** (Phase 10~19, 실제 운영 투입까지)

🔴 **코드가 이 노트북에만 있습니다.** `git remote` 가 비어 있어서 팀원이 볼 방법이 없고,
노트북이 고장나면 작업이 전부 사라집니다. 이것부터 해결합니다.

```bash
git branch -m feature/login-api main
# GitHub에서 Private 저장소 생성 후
git remote add origin https://github.com/<계정>/daroli-admin.git
git push -u origin main
```

**Claude Code에 붙여넣을 것**
```
저장소를 정리하고 GitHub에 올릴 준비를 해줘.

1. 브랜치 feature/login-api 를 main 으로 이름 변경
2. README.md 작성 — 팀원(백엔드 담당자 포함)이 클론해서 로컬에서 띄울 수 있게.
   프로젝트 설명, 실행 방법, 현재 가짜 데이터로 동작한다는 점, docs/ 문서 안내
3. CLAUDE.md 의 "현재 상태" 섹션을 Phase 9 완료 기준으로 갱신
4. .gitignore 에 빠진 게 없는지 확인 (특히 .env, .claude/)

GitHub 저장소 생성과 push 는 내가 직접 할 테니 명령어만 알려줘.
```

**같이 처리할 것:** 이 프로젝트가 OneDrive 폴더 안에 있어서 `node_modules` 가 계속 동기화됩니다.
코드 폴더만 OneDrive 밖(예: `C:\dev\daroli-admin`)으로 옮기는 걸 권합니다.

---

## 🔴 지금 바로 보낼 것 (답을 기다리는 동안 Phase 10~12 진행)

**백엔드 담당자에게** — 상태값 4개 + 읍면 저장형식 + 금액 정수 + 인증 방식
**팀·다로리인에게** — 개인정보처리자(앱 소유·운영 주체) 확정 여부

→ 문안은 `docs/운영투입_로드맵.md` 5장에 그대로 복사할 수 있게 정리해뒀습니다.
이 둘이 전체 일정의 병목입니다.

---

## ✅ Phase 9 — 완료 (2026-08-16)

`/requests` 요청 현황 완성 (기획서 2-10, 마지막 화면). 액션 없는 관제 화면 — 검색(대상자·매니저·내용)
+ 상태 필터 5종 + 목록 표. 🟨 제안 채택: 접수 24시간 경과 시 "배정 지연" 강조. mock.ts 검색 범위를
매니저명까지 확장. 브라우저에서 검색·필터·강조 전부 확인 → 커밋함.

**기획서 2장의 10개 화면이 전부 구현 완료됐습니다.**

**다음에 할 일:** 남은 건 백엔드 연결(`lib/data/real.ts`, 원래 로드맵 Phase 10)뿐인데, 이건
백엔드 담당자 확답(상태값 5개, 활동 유형 3종/5종)이 와야 시작할 수 있습니다. 그 전까지는:
- 백엔드 질문 답 오면 `lib/status.ts`의 🟨만 교체
- 필요하면 화면별 세부 다듬기(빈 상태 문구, 반응형 등)를 이어서 할 수 있음

---

## ✅ Phase 8 — 완료 (2026-08-16)

통합 지역 지도 완성 (기획서 2-9). 청도군 GeoJSON 축소(34MB→160KB, `scripts/extract-cheongdo-geojson.mjs`),
`components/region-map.tsx` 공용 컴포넌트(react-simple-maps) 작성, 대시보드 탭2·3의 placeholder를
실제 지도로 교체. 채움색·붉은테두리·라벨·호버툴팁·범례 브라우저에서 전부 확인 → 커밋함.

**다음에 할 일:** Phase 9 — `admin-requests`(요청 현황). 기획서 2-10 절 확인 후 시작할 것.
남은 화면은 이제 요청 현황 하나뿐 — 끝나면 명세에 있는 10개 화면이 모두 완성됨.

---

## ✅ Phase 7 — 완료 (2026-08-16)

대시보드 3탭(예산/매니저/돌봄 수요자) 완성 (기획서 2-6~2-8). db.getDashboard() 한 번으로 3탭 데이터 전부
조달. 6개월 추이 차트는 dataviz 스킬로 팔레트 검증(validate_palette.js 통과) 후 recharts로 구현.
지도(2-9)는 GeoJSON 없어서 placeholder. 브라우저에서 3탭 전부 데이터 정합성 확인 완료 → 커밋함.

청도군 GeoJSON 원본(전국, 34MB) 다운로드 완료 — `HangJeongDong_ver20250701.geojson` (커밋 안 함, .gitignore 처리).

**다음에 할 일:** Phase 8 — 통합 지역 지도. 먼저 원본 GeoJSON에서 청도군 9개 읍·면만 잘라내서
용량 줄이고, `react-simple-maps`로 히트맵 + 부족지역 붉은 테두리 + 호버 툴팁 구현.
그 다음 대시보드 탭2·3의 MapPlaceholder를 실제 지도로 교체.

---

## ✅ Phase 6 — 완료 (2026-08-15)

`/managers`(매니저 관리) + `/recipients`(대상자 관리) 완성 (기획서 2-4, 2-5). 매니저 심사 흐름
(applied→educated→active, 활성/비활성 전환)과 대상자 마스킹(상세주소·돌봄필요사항 항목별 클릭 해제,
전체해제 버튼 없음)·신규등록/수정/삭제 전부 브라우저에서 확인 완료. `lib/calc.ts`에 `sumWon` 추가. 커밋함.

**다음에 할 일:** Phase 7 — 대시보드 3탭(`admin-dashboard-budget/managers/recipients`). 기획서 2-6~2-8 절 확인.
지도(2-9)는 GeoJSON 없이는 만들 수 없으니 지도 없이 카드·표·차트부터. GeoJSON 준비되면 Phase 8에서 지도 붙임.

---

## ✅ Phase 5 — 완료 (2026-08-15)

`/payments`(활동비 관리) + `/settings`(설정) 완성 (기획서 2-2, 2-3). 정산 생성→승인→출금 지급/반려→승인취소
전체 흐름 브라우저에서 확인 완료. `components/confirm-dialog.tsx`·`reject-dialog.tsx` 공용 컴포넌트 추출,
review 화면도 리팩터링해서 재사용. `lib/calc.ts`에 `sumSettlements` 추가 + 테스트. 커밋함.

**다음에 할 일:** Phase 6 — `admin-managers`(매니저 관리) + `admin-recipients`(대상자 관리). 기획서 2-4, 2-5 절 확인.
2-5는 🔴 개인정보 최고 위험 화면 — 마스킹 토글 요구사항 꼭 확인할 것.

---

## ✅ Phase 4 — 완료 (2026-08-15)

`/review` 일지 검토 화면 ★ 완성 (기획서 2-1). 카드 목록 + 월/상태/매니저 필터(상태 기본값 검토 대기,
정렬 오래된 순) + 승인/반려(반려 사유 다이얼로그) + GPS없음·수동완료 배지 + 4가지 상태(로딩/정상/빈/에러).
`components/status-badge.tsx` 공용 컴포넌트 추가. 브라우저에서 승인·반려·빈 상태 전부 수동 확인 완료 → 커밋함.

**다음에 할 일:** Phase 5 — `admin-payments`(활동비 관리) + `admin-settings`(설정) ★. 기획서 2-2, 2-3 절 확인 후 시작할 것

---

## ✅ Phase 3 — 완료 (2026-08-15)

`lib/calc.test.ts` 작성 (vitest, 34개 테스트 전부 통과). devDependency로 `vitest` 추가, `npm test`로 실행.
경계 조건 위주: 잔액 0 / 잔액 초과 신청 차단 / 승인 취소 시 음수 방지 / 반려 출금 잔액 복귀 /
0으로 나누기 방지(사용률·평균지급액·가능활동수·지도채움 강도) / 부족지역 판정 비율 경계값.

**다음에 할 일:** Phase 4 — `admin-review`(일지 검토) 화면 ★. 기획서 2-1 절 확인 후 시작할 것

---

## ✅ Phase 2 — 완료 (2026-08-15)

`lib/data/mock.ts` 작성 완료 — 매니저 7명(각남면·각북면·이서면·운문면 매니저 0명), 대상자 22명(13명 미배정),
요청 15건, 활동일지 55건(수동완료 8건 포함), 정산/출금은 `lib/calc.ts`로만 집계. `lib/data/index.ts`의 `db`를
mock에 연결. 임시 스크립트로 잔액 음수 없음·지역 부족 판정·재산정 idempotent 확인 완료 → 커밋함.

**다음에 할 일:** Phase 3 — `lib/calc.ts` vitest 테스트 작성 (경계 조건: 잔액 0/초과신청/승인취소 음수/단가변경 후 재산정/합계 검증)

---

## ✅ Phase 1 — 완료 (2026-08-15)

Next.js(App Router) + TypeScript + Tailwind v4 + shadcn/ui 셋업, 사이드바 7개 메뉴 + 상단 바 placeholder,
7개 경로 빈 페이지, Pretendard 폰트 적용. 브라우저에서 7개 메뉴 이동 확인 완료 → 커밋함 (`feature/login-api`, root commit).

---

## ✅ Phase 0 — 완료 (2026-08-13)

| 파일 | 내용 |
|---|---|
| `CLAUDE.md` | Claude Code가 매 세션 자동으로 읽는 규약 |
| `lib/status.ts` | 상태값 — **백엔드 확정되면 이 파일만 고침** |
| `lib/types.ts` | 데이터 타입 (기획서 화면 명세 기준) |
| `lib/calc.ts` | 금액 계산식 (예산 4단계·잔액·지역 판정) |
| `lib/data/index.ts` | 데이터 접근 인터페이스 |
| `docs/` | 기획서·백엔드 가이드 사본 |

---

## 남은 로드맵 (상세는 `docs/운영투입_로드맵.md`)

> 2026-08-18 개정 — 와이어프레임 비교 후 **대시보드 재설계 B안 채택**. 13~15 신규 추가로 이후 번호 이동

| Phase | 할 것 | 막고 있는 것 |
|---|---|---|
| **10** | 저장소 + 팀 공유 | 없음 — **지금 바로** |
| **11** | 미리보기 배포 (가짜 데이터라 공개 안전) | Phase 10 |
| **12** | 마스킹 전역화 + 상단바 기간 필터 제거 | 없음 |
| **13** | **대시보드 재설계 (액션 큐)** ★ 신규 | 없음 |
| **14** | **지도 `/managers` 이동** ★ 신규 | Phase 13 |
| **15** | **문서 갱신 + 변경 통보** ★ 신규 | Phase 13·14 |
| **16** | 백엔드 계약 확정 | **백엔드 답변** 🔴 |
| **17** | DB 연결 ① 읽기 | Phase 16 |
| **18** | DB 연결 ② 쓰기 + 서버 가드 뚫어보기 | Phase 17 |
| **19** | 인증 연결 | 백엔드 인증 API |
| **20** | 개인정보·법적 준비 | **개인정보처리자 확정** 🔴 |
| **21** | 운영 배포 + 파일럿 + 인수인계 | Phase 19·20 둘 다 |

### 순서 제약 두 가지만 지키면 됩니다

1. **Phase 12(마스킹)는 16(DB 연결) 전에 반드시 끝난다** — DB 붙인 뒤에 만들면 실제 어르신 정보가 뜬 채로 작업하게 됩니다
2. **Phase 19(인증) 전에는 실데이터를 어디에도 배포하지 않는다**

12·13·14는 서로 의존하지 않으니 **순서를 바꿔도 됩니다**(14만 13 뒤). 셋 다 16 전에만 끝나면 됩니다.

### 🔴 Phase 9 시점에 발견된 미완성

- **인증 없음** — `middleware.ts`·`/login` 부재. 기획서 P0 1번 항목 → Phase 19
- **`/review` 에 마스킹이 하나도 없음** — 대상자 실명·활동 장소·특이사항(건강)·GPS 좌표가 그대로 노출 → Phase 12
- **상단바 2개가 Phase 1 껍데기** — 기간 필터는 제거, 마스킹 토글은 연결 → Phase 12

### 대시보드 재설계 (Phase 13) 요약

기획서 Ver 1.0의 3탭(예산/매니저/수요자)을 **"지금 처리할 일" 중심 단일 화면**으로 교체.

```
① 지금 처리할 일   화면의 60%. 8종 중 해당되는 것만. 0건이면 줄 자체를 안 그림
② 이번 달 현황     통계 타일 4개. 액션 없음
③ 활동 추이        6개월 차트, 작게
```

- 정렬 원칙: **사람이 기다리는 것 > 일이 밀리는 것 > 준비가 안 된 것**
- 명세 전체 + Claude Code 프롬프트: `docs/대시보드_재설계.md`
- ⚠️ 기획서 🟦 확정 사항 변경이므로 Phase 15에서 팀에 통보

---

## 백엔드 담당자에게 물어볼 것

**지금 당장 (이것만 오면 임시값을 확정으로 바꿉니다)**

```
일지 상태값 뭐로 내려오나요? pending / approved / rejected 로 생각하고 있는데 맞나요?
출금은 requested / paid / rejected, 정산은 calculated / approved 로 볼게요.
매니저 비활성은 inactive 맞나요?

※ applied/educated/active 랑 requested/proposed/confirmed/completed/cancelled 는
  기획서 Ver1.0에 있어서 그대로 쓰겠습니다
```

**답이 오면:** `lib/status.ts` 의 🟨 표시된 값만 고칩니다. 다른 파일은 손대지 않습니다.

나머지 질문 21개는 `docs/백엔드_소통가이드.md` 5장에 정리돼 있습니다. 급하지 않습니다.

**기획팀에 확인할 것**
- 활동 유형이 3종(일반/공동체/긴급)인지 5종(+픽업/기타)인지
- 요청 유형 ↔ 활동 유형 변환 규칙

---

## 내가 준비할 것

- [x] ~~청도군 9개 읍·면 GeoJSON~~ — Phase 8에서 완료 (34MB → 160KB)
- [ ] **백엔드 담당자에게 질문 보내기** (상태값·읍면형식·금액·인증) — 병목 1
- [ ] **개인정보처리자 확정 확인** — 병목 2. 실데이터 투입의 전제조건
- [ ] GitHub 비공개 저장소 생성 + 백엔드 담당자 초대
- [ ] 코드 폴더를 OneDrive 밖으로 이동 (node_modules 동기화 문제)

---

## 세션 시작 루틴

1. `git status` 로 깨끗한지 확인
2. **이번 세션에 만들 화면 하나**를 정한다
3. `docs/관리자대시보드_제작기획.md` 에서 그 화면 명세를 복사해 프롬프트에 붙인다
4. 계획 먼저 받고 → 읽고 → 고치고 → 실행
5. 브라우저 확인 → 커밋 → 이 파일에 다음 할 일 한 줄 적기
